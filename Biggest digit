#include <stdio.h>

int main()
{
int i,n,r,max=0;
printf("enter the number");
scanf("\n%d",&n);
while(n!=0)
{
  r=n%10;
  if(r>max)
  {
      max=r;
  }
  n=n/10;
}
printf("%d",max);
return 0;
}